'use strict';

/**
 * Realise sum function:
 * Function gets 2 numbers and return it's sum
 *
 * @param {number} a
 * @param {number} b
 *
 * @return {number}
 */

function sum(a, b) {
  return a + b;
}

module.exports = sum;
